---
layout: default
tags:
  - challenge
eleventyNavigation:
  key: Home
  order: -1
eleventyComputed:
  title: "{{ today.toLocaleDateString('en-US', { month: 'long', day: 'numeric', year: 'numeric' }) }}"
  date: "{{ today.toISOString() }}"
---

<h2>Melody</h2>

<dl>
  <dt>Phrase structure</dt>
  <dd>
    <ol>
      {% for phrase in form.form | list | unique %}
        <li>{{ phrase }}: {{ form.phrase | pick }}</li>
      {% endfor %}
    </ol>
  </dd>
</dl>

<h2>Harmony</h2>

<dl>
  <dt>Key</dt>
  <dd>{{ tonics | pick }} {{ scales | pick }}</dd>
  <dt>Harmonic language</dt>
  <dd>{{ harmonicLanguage | pick }}</dd>
</dl>

<h2>Rhythm</h2>

<dl>
  <dt>Tempo</dt>
  <dd>{{ tempo }} BPM</dd>
  <dt>Meter</dt>
  <dd>{{ meter.numerator | pick }}/{{ meter.denominator | pick }}</dd>
  <dt>Rhythmic feature</dt>
  <dd>{{ rhythmicFeature | pick }}</dd>
</dl>

<h2>Texture</h2>

<dl>
  <dt>Texture</dt>
  <dd>{{ textures | pick }}</dd>
  <dt>Emotion</dt>
  {% set emotion1 = emotions | pick %}
  {% set emotion2 = emotions | pick %}
  <dd>{{ [emotion1, emotion2] | unique | join(' + ')}}</dd>
</dl>

<h2>Arrangement</h2>

<dl>
  <dt>Form</dt>
  <dd>{{ form.form }}</dd>

  {% set arrFeat = arrangementFeature | pick %}

  {% if arrFeat %}
    <dt>Arrangement feature</dt>
    <dd>{{ arrFeat }}</dd>
  {% endif %}
</dl>
